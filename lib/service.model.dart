class Service{

  int id;
  String serviceName;
  String lastRun;
  int successCount;
  int errorCount;

  Service({ this.id, this.serviceName, this.lastRun, this.successCount, this.errorCount});
  
}